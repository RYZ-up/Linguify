{
  "rules": {
    "leaderboard": {
      ".read": true,
      ".write": "auth != null",
      ".indexOn": ["score"]
    },
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    }
  }
}
